

# 处理松弛哥误领张艳邀请码

## 背景

- **松弛哥**（用户 ID: `13807a48-...`，手机 `18611686886`）误点了张艳的邀请链接 `BLOOM-ZY01`，通过手动输入邀请码完成了领取
- 松弛哥本人也是绽放合伙人，保留其绽放身份和权益不变
- **张艳**（手机 `13726679755`）目前尚未在系统中注册账号

## 处理方案

### 步骤 1：重置张艳的邀请码为可领取状态

将 `BLOOM-ZY01` 的状态从 `claimed` 改回 `pending`，清除领取记录：

```text
UPDATE partner_invitations 
SET status = 'pending', claimed_by = NULL, claimed_at = NULL 
WHERE invite_code = 'BLOOM-ZY01';
```

### 步骤 2：无需其他操作

- 松弛哥的绽放合伙人身份、bloom_partner_orders、财富测评权益、7天训练营权益均保留不动
- 张艳注册账号后，可通过以下方式领取：
  - **自动匹配**：如果她用手机号 `13726679755` 注册，系统会自动匹配并发放权益
  - **手动输入**：在页面输入邀请码 `BLOOM-ZY01` 领取

## 技术说明

只需执行 1 条数据库更新语句，不涉及代码改动。松弛哥的 partner 记录和权益数据与邀请码无关联依赖，重置邀请码不会影响他的任何权益。

