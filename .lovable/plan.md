

## 去除用户见证 + 加大训练营按钮

### 修改内容

**文件**：`src/components/wealth-block/AwakeningJourneyPreview.tsx`

1. **删除**第 231-242 行的"简化的用户见证"区块（包含 📈 和 testimonial 引用）
2. **加大训练营按钮**：
   - 按钮高度从 `h-12` 增大到 `h-14`
   - 字体从 `text-sm` 改为 `text-base`
   - 增加更明显的阴影 `shadow-xl`
   - 让训练营按钮在移动端占满宽度，"了解详情"按钮缩小为次要样式
3. 如果 `testimonial` 相关数据定义不再被引用，一并清理

