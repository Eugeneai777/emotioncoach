# 创建"身份绽放训练营"售前落地页

## 概述

基于现有 `SynergyPromoPage.tsx`（7天有劲训练营，约1050行）的完整框架，创建一个新的 `IdentityBloomPromoPage.tsx`，替换所有内容为"身份绽放训练营"专属信息。页面结构、交互模式、视觉风格完全复用，仅内容和配色微调。

## 页面结构（5大板块）

### 1. Hero Section

- 标题：**身份绽放训练营**
- 副标题口号：直击身份焦虑/事业瓶颈/能量不足
- 三引擎公式：AI教练 + 专业教练 + 知乐胶囊（4瓶）
- 价格展示：原价5280 → **活动价 ¥3980**
- 配色：深紫到靛蓝渐变（区别于synergy的琥珀色），体现沉稳高端
- CTA按钮："立即报名，开启蜕变" / 兑换码激活（复用现有逻辑）

### 2. 痛点与承诺 Section

- 复用 `IdentityBloomIntroSections.tsx` 中已有的6条痛点（找不到自己、不敢拒绝、旧模式等）
- 新增承诺文案：训练营如何解决这些问题

### 3. 课程详情与收获 Section

- **4阶段 × 每阶4节课时间线**：复用已有的 stages 数据（我知道我是谁 → 自主生命 → 突破迷雾 → 转化困境）
- 每阶段：4节音频课 + 4次教练课
- "你将获得"三层金字塔：核心交付（AI教练系统 + 16节音频课 + 4瓶知乐胶囊）、专属权益（1V1教练 + 海沃塔研讨）、附加福利

### 4. 师资与信任状 Section

- **完全复制** SynergyPromoPage 的师资数据（黛汐 + 5位教练 + 支持角色）
- 知乐胶囊产品资质区块：复用现有产品图、规格、认证信息
- 强调"本训练营包含4瓶知乐胶囊"

### 5. 价值重申与行动号召 Section

- 总结：¥3980 = 16节音频课 + 16次教练课 + 4瓶知乐胶囊 + 社群
- Sticky底部栏 + 兑换码弹窗（复用 `SynergyRedeemDialog`）
- 分享海报功能

## 技术实现

### 新建文件


| 文件                                     | 说明                         |
| -------------------------------------- | -------------------------- |
| `src/pages/IdentityBloomPromoPage.tsx` | 主页面，基于 SynergyPromoPage 结构 |


### 修改文件


| 文件            | 说明                           |
| ------------- | ---------------------------- |
| `src/App.tsx` | 新增路由 `/promo/identity-bloom` |


### 关键差异点（vs SynergyPromoPage）

1. **配色**：从琥珀/橙色系 → 靛蓝/紫色系（匹配identity_bloom品牌色）
2. **内容**：痛点、课程详情全部替换为身份绽放内容
3. **价格**：¥3980（含4瓶知乐胶囊），展示原价5280划线
4. **训练营类型**：`identity_bloom` 替代 `emotion_stress_7`
5. **购买校验**：检查 `identity_bloom` 相关订单
6. **师资部分**：完全复制，零修改

### 购买/激活流程

- 复用 `SynergyRedeemDialog` 兑换码逻辑
- 购买状态检查：查询 `orders` 表 `package_key = 'identity_bloom'`
- 成功后自动创建 `identity_bloom` 类型训练营并跳转打卡页。