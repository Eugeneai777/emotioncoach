

# 修复身份绽放落地页三个问题

## 问题

1. **"六大核心亮点"标题错误**：`coreHighlights` 数组只有 5 项，但标题写的是"六大核心亮点"，需改为"五大核心亮点"。
2. **"专属亮点"区块的"资深教练小组辅导"卡片居中对齐不够美观**：当前用 `flex justify-center`，需要让卡片宽度和上方核心交付卡片对齐（占满容器宽度）。
3. **"¥3980 你将获得"价值汇总区（L952-959）仍包含"AI身份教练"和"海沃塔研讨"**：需删除这两项，同时底部引语文案（L974-977）中的"AI教练+专业教练+知乐胶囊三重陪伴"需改为"专业教练+知乐胶囊双重陪伴"。

## 改动

### `src/pages/IdentityBloomPromoPage.tsx`

**① L616**：`六大核心亮点` → `五大核心亮点`

**② L710-730 区域（专属亮点渲染）**：将单卡片从 `flex justify-center` 改为全宽布局，与上方核心交付卡片样式对齐（使用相同的 `border-l-[3px]` 卡片样式）。

**③ L952-959（价值汇总网格）**：删除 `AI身份教练` 和 `海沃塔研讨` 两项，保留4项：16节音频课、16次教练课、知乐胶囊×4瓶、成长社群。

**④ L974-977（底部引语）**：
```
"从迷失到绽放，从不敢到勇敢，
专业教练+知乐胶囊双重陪伴，
帮你找回真实的自己，活出生命的力量。"
```

## 涉及文件

| 文件 | 操作 |
|------|------|
| `src/pages/IdentityBloomPromoPage.tsx` | 4处文案/布局修改 |

