## 男人身体警报扫描（V6 最终落地版）

### 一、加微钩子最终方案（方案 B）

4 张「个性化身体信号长图」，每档一张，1 屏可看完，结尾带回话钩子。**取代原"手册"概念**。

| 档位 | 长图主标题 | 1 屏内容结构 | 回话钩子 |
|---|---|---|---|
| 🟢 0-7 警报静默 | 你的身体目前在低噪运行 | 1 张主视觉 + 3 条"保持清单"（睡/动/性）+ 1 句风险预警 | "回复『保持』领取 7 天日常清单" |
| 🟡 8-14 黄色警报 | 身体在小声提醒你 3 件事 | 3 个高亮信号词（如：晨起沉、性致低、夜醒）+ 每个 1 句白话解释 | "回复你最在意的那一条（如『夜醒』），我发你同龄人怎么处理的" |
| 🟠 15-22 橙色警报 | 你身上 5 个信号已经在叠加 | 5 个信号词聚合图 + 1 句"同档位 38% 的人 3 个月后……" | "回复『橙色』领 15 分钟陪聊名额（本周限 20 个）" |
| 🔴 23-30 红色警报 | 不是矫情，是身体在喊救命 | 大字"立即"+ 3 个最紧急信号 + 1 句"先做这 1 件事" | "回复『红色』，今天内回拨你电话" |

**长图规格**：750×1334（1 屏手机），每张 ≤120 字，主视觉占 60%。

**制作方式**：`imagegen` 生成 4 张主视觉底图（中年男性意象 + 警报色系），文案我撰写并经你审核，再用前端 html2canvas 合成最终长图（与现有海报组件同栈，0 新依赖）。

### 二、完整测评配置

- **assessment_key**：`male_unspoken_check`
- **外显标题**：男人身体警报扫描
- **副标**：10 道题，看看你身上那些"说不出口"的信号到底有多响
- **题目**：3 维 10 题 30 分
  - `somatic_alarm`（4 题/12 分）：身体警报信号
  - `unspoken_male`（4 题/12 分）：包含 Q8 亲密心理 + D 选项
  - `intimacy_pressure`（2 题/6 分）：亲密关系压力
- **4 档结果**：0-7 / 8-14 / 15-22 / 23-30

### 三、UI 复用 vs 重写

| 模块 | 处理 |
|---|---|
| 测评引擎（DynamicAssessmentPage） | 复用 |
| Intro 详情页 + 顶部「查看历史/再测一次」 | 复用，再测加 7 天冷却提示 |
| 底部「限时免费开始评估 / 查看历史」双 CTA | 复用 |
| 分享海报 | 复用组件，文案全部重写（合规） |
| 结果页分享按钮 | 复用 |
| 结果页加微钩子 | **新组件 `BodySignalHookCard`**，按 4 档渲染不同长图 + 文案 |
| MaleVitalityCompareView / CampPrimaryCTA | 不复用 |

### 四、代码侧 6 处挂点

1. `ASSESSMENT_PROMO_CONFIGS` 增 `male_unspoken_check` 配置
2. `DynamicAssessmentIntro.tsx` 增 hero + 双 CTA 分支
3. `DynamicAssessmentPage.tsx` 加入 `FORCE_AUTH_KEYS`
4. `DynamicAssessmentResult.tsx` 渲染新 `BodySignalHookCard`
5. 新建 `BodySignalHookCard.tsx`（4 档长图 + 回话钩子）
6. 新建 `MaleUnspokenShareCard.tsx`（合规版分享海报）

### 五、执行清单（开干后顺序）

1. `ls public/assessment-media/` 盘点已有素材
2. `imagegen` 生成缺失测评媒资 + 4 张钩子长图底图（共 ≤7 张）
3. `supabase--insert` 写入 `partner_assessment_templates`（10 题 JSON + 4 档结果 + AI 护栏 prompt）
4. 6 处代码挂点
5. 浏览器全链路验证：详情页 → 10 题 → 4 档结果 → 4 档钩子长图 → 分享海报

### 六、待你确认

1. 4 档钩子文案与回话关键词（上表）是否 OK？
2. 加微二维码：复用现有"7 天有劲训练营"教练企微，还是另起一个？
3. "本周限 20 个" / "今天内回拨" 是真实可兑现，还是要换成更安全的话术？

回复 **开干** 或带上修改点即开始执行。
