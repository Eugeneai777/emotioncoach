
# 有劲训练营后台管理系统方案

## 现状分析

### 数据库层

`camp_templates` 表已包含完整的训练营配置字段：

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| id | uuid | 主键 |
| camp_type | text | 训练营类型标识 |
| camp_name | text | 训练营名称 |
| camp_subtitle | text | 副标题 |
| description | text | 描述 |
| duration_days | integer | 天数 |
| price | numeric | 现价 |
| original_price | numeric | 原价 |
| price_note | text | 价格标签 |
| benefits | jsonb | 权益列表 |
| stages | jsonb | 阶段课程 |
| target_audience | jsonb | 目标人群 |
| category | text | 分类（youjin/bloom） |
| is_active | boolean | 是否启用 |
| display_order | integer | 排序 |

### 问题

- **无管理界面**：训练营数据只能通过数据库直接修改
- **无法可视化管理**：价格、权益、阶段配置需手动编辑 JSON

---

## 解决方案

创建「训练营管理」后台页面，支持：

1. **基础信息管理**：名称、副标题、描述、分类
2. **价格配置**：现价、原价、价格标签
3. **权益配置**：可视化编辑 benefits 数组
4. **阶段配置**：编辑 stages 课程结构
5. **状态控制**：启用/禁用、排序

---

## 实现方案

### 1. 新建管理组件

**文件**: `src/components/admin/CampTemplatesManagement.tsx`

```
┌──────────────────────────────────────────────────────┐
│  训练营管理                                           │
├──────────────────────────────────────────────────────┤
│  [有劲训练营] [绽放训练营]                            │
├──────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────┐  │
│  │ 📓 21天情绪日记训练营                          │  │
│  │ ¥299 (原价¥399) · 21天 · 已启用               │  │
│  │ [编辑] [禁用]                                  │  │
│  └────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │ 💰 7天财富觉醒训练营                           │  │
│  │ ¥299 (原价¥399) · 7天 · 已启用                │  │
│  │ [编辑] [禁用]                                  │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

### 2. 编辑对话框

```
┌──────────────────────────────────────────────────────┐
│  编辑训练营: 21天情绪日记训练营                        │
├──────────────────────────────────────────────────────┤
│  [基础信息] [价格设置] [权益配置] [阶段课程]           │
├──────────────────────────────────────────────────────┤
│  基础信息 Tab:                                        │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 训练营名称: [21天情绪日记训练营        ]        │ │
│  │ 副标题:     [系统化情绪管理训练          ]      │ │
│  │ 图标:       [📓]                                │ │
│  │ 天数:       [21]                                │ │
│  │ 分类:       [youjin ▼]                          │ │
│  │ 描述:       [多行文本框...                ]     │ │
│  │ 是否启用:   [✓]                                 │ │
│  └─────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────┤
│  价格设置 Tab:                                        │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 现价:       [299     ]                          │ │
│  │ 原价:       [399     ] (用于划线价显示)         │ │
│  │ 价格标签:   [限时优惠 ]                         │ │
│  └─────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────┤
│  权益配置 Tab:                                        │
│  ┌─────────────────────────────────────────────────┐ │
│  │ [1] 每日AI情绪对话                 [删除]       │ │
│  │ [2] 专属情绪日记模板               [删除]       │ │
│  │ [3] 情绪可视化分析                 [删除]       │ │
│  │ [+ 添加权益]                                    │ │
│  └─────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────┤
│                            [取消] [保存]              │
└──────────────────────────────────────────────────────┘
```

### 3. 路由配置

**修改文件**: `src/pages/Admin.tsx`

```typescript
import { CampTemplatesManagement } from "@/components/admin/CampTemplatesManagement";

// 在 AdminRoutes 中添加
<Route path="camps" element={<CampTemplatesManagement />} />
```

### 4. 侧边栏入口

**修改文件**: `src/components/admin/AdminSidebar.tsx`

在「内容管理」分组中添加：

```typescript
{ key: "camps", label: "训练营管理", path: "/admin/camps", icon: Tent }
```

---

## 技术细节

### 数据结构

**benefits 字段示例**:
```json
["每日AI情绪对话", "专属情绪日记模板", "情绪可视化分析", "打卡激励系统"]
```

**stages 字段示例**:
```json
[
  { "stage": 1, "title": "觉察期", "lessons": ["认识情绪", "情绪命名"] },
  { "stage": 2, "title": "理解期", "lessons": ["情绪来源", "触发识别"] },
  { "stage": 3, "title": "掌控期", "lessons": ["情绪调节", "习惯养成"] }
]
```

### 组件结构

```
CampTemplatesManagement.tsx
├── CampListTable (训练营列表)
├── CampEditDialog (编辑对话框)
│   ├── BasicInfoTab (基础信息)
│   ├── PricingTab (价格设置)
│   ├── BenefitsEditor (权益编辑器)
│   └── StagesEditor (阶段编辑器)
└── AddCampDialog (新增训练营)
```

---

## 修改文件清单

| 文件 | 操作 | 说明 |
|:-----|:-----|:-----|
| `src/components/admin/CampTemplatesManagement.tsx` | 新增 | 训练营管理主组件 |
| `src/pages/Admin.tsx` | 修改 | 添加路由 |
| `src/components/admin/AdminSidebar.tsx` | 修改 | 添加侧边栏入口 |

---

## 功能特性

1. **分类Tab切换**：有劲/绽放训练营分开管理
2. **价格即时编辑**：修改后实时生效，前端自动更新
3. **权益可视化**：拖拽排序、添加删除
4. **阶段课程配置**：多阶段课程结构管理
5. **状态控制**：一键启用/禁用训练营
6. **排序调整**：拖拽或数值调整显示顺序

---

## 效果预览

修改完成后，管理员可在 `/admin/camps` 页面：

- 查看所有训练营及其状态
- 修改价格（现价、原价、价格标签）
- 编辑权益列表
- 配置阶段课程
- 控制训练营启用/禁用
- 调整显示顺序

前端页面（如 `/camp-list`）将自动展示最新配置，无需修改代码。
