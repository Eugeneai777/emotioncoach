

## Plan: Hide Floating Rocket Button on /my-page

Add `'/my-page'` to the `EXCLUDED_ROUTES` array in both:

1. **`src/components/FloatingVoiceButton.tsx`** (line 15) — add `'/my-page'` to the array
2. **`src/components/FloatingQuickMenu.tsx`** (line 15) — add `'/my-page'` to the array

Single-line change in each file.

