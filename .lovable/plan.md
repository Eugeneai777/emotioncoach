我会把 `/assessment/male_midlife_vitality` 接入中年觉醒专区 `/laoge` 的测评模块，并保持当前老哥AI页面的中年男性调性与转化层级。

实施方案：

1. 调整 `/laoge` 测评模块结构
   - 在“聊完再做个体检”模块中加入“男性活力状态测评”。
   - 保留现有财富卡点、情绪健康、觉醒力入口，不删除原有入口。
   - 因测评数量从 3 个变为 4 个，移动端不再强行三列拥挤，改为更稳定的 2 列卡片网格；桌面/宽屏仍保持居中、卡片尺寸稳定。

2. 新增测评入口卡片
   - 入口跳转到：`/assessment/male_midlife_vitality?source=laoge`
   - 卡片文案建议：
     - 标题：男性活力
     - 描述：18题·约4分钟
     - 标签：状态盘点 / 专业版（根据页面已有价格/付费状态保持一致）
   - 视觉上使用低刺激、私密、克制的表达，避免“性功能”等过强医疗化或尴尬词汇，符合中年男性公众号用户体验。

3. 购买/完成状态判断
   - 如果该动态测评配置了付费包名，会把对应 `package_key` 加入 `/laoge` 的批量购买检查。
   - 若用户已购买该测评，则可选择隐藏入口或显示“已解锁”状态；我会优先采用与现有模块一致的逻辑：未完成/未解锁时展示，避免重复转化。
   - 现有 `orders(status='paid')` 作为购买状态来源，不引入新的本地状态表。

4. 优化整体排版兼容
   - 调整测评模块网格、卡片间距、字号和标签布局，避免 4 个卡片在手机端拥挤。
   - 保证 `/laoge` 顶部、免费聊天模块、系统方案模块不受影响。
   - 电脑端保持 `max-w-lg` 居中风格，不做大幅改版。

5. 控制影响范围
   - 主要修改 `src/pages/LaogeAI.tsx`。
   - 不修改测评题目、评分逻辑、海报生成逻辑、结果页逻辑、支付逻辑。
   - 不改数据库结构。

验收效果：
- 用户进入 `/laoge` 后能在测评模块看到“男性活力状态测评”入口。
- 点击后进入 `/assessment/male_midlife_vitality`，并保留来源参数便于后续追踪。
- 手机端 4 个测评卡片排版清晰，不拥挤、不遮挡。
- 整体文案和视觉仍保持“老哥AI/中年觉醒专区”的克制、轻松、私密感。