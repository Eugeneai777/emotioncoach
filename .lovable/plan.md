

## 优化飞轮卡片 - 更紧凑 + 布局调整

### 改动内容

#### 1. MyFlywheelOverview.tsx - 标题位置互换 + 重命名 + 紧凑化

**标题区域布局调整**（第 168-203 行）：
- 将 "我的飞轮" 改为 **"我的财富飞轮"**，放到**左边**
- 合伙人身份标签（绽放合伙人 · 30%+10%、初级合伙人 · 18%）移到**右边**
- 调整为 `flex-row-reverse` 或直接重新排列 DOM 顺序

**4 格漏斗卡片紧凑化**（第 206-241 行）：
- 内边距从 `p-3` 减为 `p-2.5`
- 数字字号从 `text-2xl` 减为 `text-xl`
- 格间距从 `gap-3` 减为 `gap-2`
- Level badge 圆形从 `w-6 h-6` 减为 `w-5 h-5`

**底部汇总行**（第 256-265 行）：
- 内边距从 `pt-2` 减为 `pt-1.5`
- 可提现颜色从 `text-green-600` 改为 `text-orange-600`（统一橙色）

**卡片整体**：
- CardContent padding 从 `p-4` 减为 `p-3`
- 垂直间距从 `space-y-4` 减为 `space-y-3`

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/components/partner/MyFlywheelOverview.tsx` | 标题重命名+位置互换+紧凑化 |

### 无数据库改动

纯前端样式调整，改 1 个文件。
