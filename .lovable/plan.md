

# 添加页面标题"宝妈AI助手"

在 `src/pages/MamaAssistant.tsx` 顶部添加 `PageHeader` 组件，显示标题"宝妈AI助手"。

## 改动

**`src/pages/MamaAssistant.tsx`**：
1. 引入 `PageHeader`
2. 在 `<MamaHero />` 前添加 `<PageHeader title="宝妈AI助手" />`

单文件改动，两行代码。

