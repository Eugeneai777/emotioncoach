执行一个小范围、定向修复，只影响 `camp-emotion_stress_7` 在微信支付成功后的成功态展示与页面按钮状态，不改动其它产品的游客支付激活逻辑。

## 修改范围

1. `src/components/WechatPayDialog.tsx`
   - 新增一个仅用于识别训练营特殊场景的小判断：`packageInfo.key === 'camp-emotion_stress_7'`。
   - 在订单轮询或回前台补偿校验拿到 `paid` 后：
     - 如果是 `camp-emotion_stress_7`，不再进入 `guest_success` 分支。
     - 弹框直接显示普通 `success` 状态：只展示“支付成功”。
     - toast 文案改为“支付成功！”。
     - 按现有流程触发 `onSuccess()`，让页面刷新权益并进入训练营。
   - 其它套餐仍保持现有逻辑：未登录游客支付成功后继续显示“请登录或注册以激活权益”。

2. `src/pages/CampIntro.tsx`
   - 增加本地即时成功状态，例如 `paymentCompleted`。
   - `hasPurchased` 改为：数据库订单已支付 OR 本次支付刚成功。
   - 支付成功回调里先设置本地成功态，确保页面按钮马上从“立即购买 ¥399”变成“开始训练”。
   - 仅对 `emotion_stress_7` 保持现有自动开营逻辑，成功后进入训练营。

## 不改动内容

- 不改数据库结构。
- 不改支付回调函数。
- 不改支付宝支付逻辑。
- 不改其它训练营、测评、会员套餐的游客支付后登录激活逻辑。
- 不改变 `orders` 作为购买事实来源的规则。

## 验证方式

- 类型检查通过。
- 重点检查两条路径：
  1. 微信浏览器支付 `camp-emotion_stress_7` 成功后，只显示“支付成功”，页面按钮变为“开始训练”或自动进入训练营。
  2. 非该套餐的游客支付成功仍显示登录/注册激活提示，避免影响其它业务。