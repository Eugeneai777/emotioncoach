

## 优化底部按钮：去掉副标题，降低按钮高度

### 修改内容

**文件：`src/components/coach/CoachEmptyState.tsx`**

1. 删除"专属财富教练，为你量身定制"这行副标题文字
2. 按钮高度从 `h-14`（56px）降为 `h-12`（48px）
3. 容器的上下 padding 适当缩小（`pt-4` 改为 `pt-2`，底部 padding 保持安全区域适配）

修改后按钮区域更紧凑，不会遮挡上方内容。

