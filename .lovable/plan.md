

## 在落地页详情页添加 AI 设计优化功能

### 现状分析

当前落地页系统的局限：
- **文案层**：`content_a/content_b` 只存储文字内容（title, subtitle, selling_points, cta_text, cta_subtext）
- **设计层**：`LandingPage.tsx` 使用硬编码的视觉样式 — 固定渐变背景、固定配色、固定布局结构
- **AI 能力**：`flywheel-landing-page-ai` 边缘函数只生成文案，不涉及设计参数

### 方案概述

扩展落地页内容结构，新增 `design` 字段存储 AI 生成的视觉设计参数，并在详情页增加"AI 优化"面板，支持同时优化文案和设计。

### 具体改动

**1. 数据库：`partner_landing_pages` 表新增 `design` 字段**

添加 `design jsonb` 列，存储视觉参数：

```text
{
  "theme": "warm" | "cool" | "energetic" | "calm" | "professional",
  "bg_gradient": "from-rose-50 via-orange-50 to-amber-50",
  "accent_color": "#E11D48",
  "card_style": "glass" | "solid" | "outline",
  "cta_gradient": "from-rose-500 to-orange-500",
  "text_color": "dark" | "light",
  "layout": "centered" | "left-aligned",
  "decorative": "circles" | "waves" | "dots" | "none"
}
```

**2. 修改边缘函数 `flywheel-landing-page-ai/index.ts`**

- `generate` 模式：AI 同时生成文案 + 设计参数，返回 `{ content_a: { ...文案, design: {...} }, content_b: { ...文案, design: {...} } }`
- `optimize` 模式：接受设计相关指令（如"换成暖色调"、"背景更活泼"），返回更新后的完整内容含 design
- 系统提示词增加设计维度：要求 AI 根据目标人群和产品特性推荐配色方案、布局风格

**3. 修改 `src/pages/LandingPage.tsx` — 动态渲染设计**

将硬编码样式替换为从 `design` 字段读取的动态值：
- 背景渐变：`bg_gradient` 替代固定的 `from-primary/5 via-background`
- 强调色：`accent_color` 控制勾选标记、图标、按钮颜色
- 卡片样式：`card_style` 控制毛玻璃/实心/描边效果
- CTA 按钮：`cta_gradient` 控制按钮渐变色
- 装饰元素：`decorative` 控制背景光晕形状
- 提供安全降级：无 design 字段时保持现有默认样式

**4. 修改 `src/components/partner/PartnerLandingPageDetail.tsx` — AI 优化面板**

在详情页"复制链接/预览"按钮旁新增"AI 优化"按钮，点击后展开底部面板：
- 左侧：AI 对话区（输入指令如"换成蓝色科技风"、"标题更有冲击力"）
- 右侧：迷你实时预览（展示设计 + 文案变化）
- 底部：快捷设计指令按钮（"暖色温馨"、"冷色专业"、"活力明亮"、"简约高级"）
- "应用修改"按钮保存文案 + 设计到数据库

**5. 修改 `src/components/partner/AILandingPageWizard.tsx` — 创建流程也生成设计**

Step 3（A/B 内容预览）中展示带设计风格的预览卡片，Step 4（对话优化）支持设计指令。

### 交互流程

```text
详情页 → 点击 "AI 优化" → 展开对话面板
  |
  |- 用户输入："换成暖色调，标题更有紧迫感"
  |
  |- AI 返回：{ title: "...", design: { bg_gradient: "...", accent_color: "..." } }
  |
  |- 预览区实时展示新设计 + 新文案
  |
  |- 快捷按钮：🌸 暖色温馨 | 🧊 冷色专业 | ⚡ 活力明亮 | 🖤 简约高级
  |
  +- 点击 "应用修改" → 保存到数据库 → 刷新预览
```

### 预设设计主题参考

| 主题 | 背景 | 强调色 | 卡片 | 适用场景 |
|------|------|--------|------|----------|
| warm | rose-50 → amber-50 | #E11D48 | glass | 女性情感/亲子 |
| cool | blue-50 → indigo-50 | #2563EB | solid | 专业测评/职场 |
| energetic | yellow-50 → orange-50 | #EA580C | outline | 活动推广/限时 |
| calm | green-50 → teal-50 | #0D9488 | glass | 冥想/放松/睡眠 |
| professional | slate-50 → gray-50 | #475569 | solid | 商务/咨询 |

### 改动文件汇总

| 文件 | 改动 |
|------|------|
| 数据库迁移 | `partner_landing_pages` 新增 `design jsonb` 列 |
| `supabase/functions/flywheel-landing-page-ai/index.ts` | generate/optimize 模式增加设计参数生成 |
| `src/pages/LandingPage.tsx` | 动态读取 design 字段渲染视觉样式 |
| `src/components/partner/PartnerLandingPageDetail.tsx` | 新增 AI 优化面板（对话 + 快捷设计 + 预览） |
| `src/components/partner/AILandingPageWizard.tsx` | 创建流程支持设计参数 |

