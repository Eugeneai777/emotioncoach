## 朋友圈竖版海报优化：嵌入真实直播间二维码

### 改动范围
仅修改 `/tmp/make_posters.py` 中朋友圈竖版海报（1080×1440）部分。小鹅通封面不动。

### 具体改动

1. **生成真实二维码**
   - 用 Python `qrcode` 库（如未安装则 `python -m pip install qrcode[pil]`）
   - 编码 URL：`https://app0mjygpak6238.h5.xiaoecloud.com/v4/course/alive/l_6a164516e4b0694c350f6511?app_id=app0mjYgPak6238&alive_mode=0&pro_id=&type=2`
   - 参数：`box_size=10, border=2, error_correction=ERROR_CORRECT_M`，输出尺寸约 460×460，确保扫码清晰
   - 黑色码点 + 纯白背景（保证识别率，不做花式着色）

2. **替换底部占位框**
   - 把原"福利官二维码占位"白底 380×380 框替换为：
     - 白色圆角卡片（420×500，圆角 24px）
     - 上方：真实二维码（400×400，居中）
     - 下方文案改为 **"长按识别 · 进入直播间"**（28pt 思源黑体，深色 `#2a1f3d`）
   - 顶部小标签：去掉"福利官1:1答疑"那条收获要点中与福利官二维码强相关的措辞？→ 不改文案，仅改底部二维码区

3. **保留不变**
   - 标题/副标题/双教练卡片/4 条收获要点/时间卡/slogan/配色/字体
   - 海报尺寸仍为 1080×1440，输出 JPG，<800KB

### 输出
- 覆盖更新 `/mnt/documents/男人夜话-朋友圈海报-1080x1440.jpg`
- QA：用 code--view 打开新海报，确认二维码清晰居中、文案对齐、整体无错位；然后 `<presentation-artifact>` 交付

### 风险点
- 直播链接较长，二维码密度较高，必须保证 ≥380px 渲染尺寸 + 白边 ≥20px，否则微信识别失败。已按 400×400 + 白色卡片留白预留。
