/**
 * BloomInvitePrompt — disabled.
 * Previously showed a bloom partner invite-code dialog after login.
 * Kept as a no-op so App.tsx imports remain valid.
 */
export function BloomInvitePrompt() {
  return null;
}
